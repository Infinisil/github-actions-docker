on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    container: nixpkgs/nix:latest
    steps:
      - uses: actions/checkout@v2.3.3
      - run: nix-shell -p hello --run hello
